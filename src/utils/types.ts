export interface DataManagerUserProp {
    key: number | string;
    name: string;
    creator: string;
    createdDate: string;
}
export interface DataListUserProp {
    key: number | string;
    name: string;
    email: string;
    role: string;
    dateActived: string;
    status: boolean;
}
