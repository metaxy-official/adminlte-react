export interface DataManagerUserProp {
    key: number | string;
    name: string;
    creator: string;
    createdDate: string;
}
export interface DataType {
    key: number | string;
    name: string;
    creator: string;
    createdDate: string;
}
